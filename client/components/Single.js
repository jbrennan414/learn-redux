import React from 'react';

const Single = React.createClass({
    render(){
        return (
            <div className="photo-grid">
                I'm the single
            </div>
        )
    }
})

export default Single;